# DOC-004 — Risk Register

## Document control

- Established: 2026-07-24
- Scope: Controlled MVP development risks
- Review rule: Update in the task that identifies, changes, accepts, or closes
  a risk

| Risk ID | Risk | Impact | Current controls | Residual status | Owner decision / next action |
|---|---|---|---|---|---|
| RSK-0001 | The explicit legacy compatibility FastAPI application could be deployed or exposed mistakenly | Unsupported, unauthenticated legacy output could be treated as supported evidence or cross the SaaS boundary | Default composition is health-only; legacy composition is explicitly named and warned; deterministic import/route tests | OPEN — HIGH | Owner accepted retention for characterization in DEC-0003; add deployment/CI denial controls in a later approved task |
| RSK-0002 | FastAPI/Starlette TestClient emits a third-party deprecation warning | A future dependency update could break backend tests or mask new warnings | Warning is recorded; current tests pass deterministically | OPEN — LOW | Owner accepted as technical debt in DEC-0003; resolve during dependency/quality task without weakening tests |
| RSK-0003 | DEV-0201 path inspection cannot eliminate filesystem time-of-check/time-of-use replacement | A candidate could change after inspection if later code relies only on the returned path | Adapter is not API-exposed and performs no parsing; link/reparse and root escape fail closed; result makes no support claim | OPEN — HIGH | Evidence-source registration, immutable storage, hashing, and verified working-copy tasks must bind later processing to controlled material |
| RSK-0004 | Apple does not publicly document the internal local-backup manifest schema used by the proposed validator | Provisional rules could reject valid backups or accept incompatible structures | FOR-007 labels rule basis, uses schema characteristics rather than iOS allowlists, and blocks implementation pending owner approval and fixture evidence | OPEN — HIGH | Owner must approve/revise profile and required fixture basis before Stage B |
| RSK-0005 | A filesystem copy of main/WAL/journal files may be inconsistent if the source changes during or between reads | Integrity conclusions could be invalid despite individual hashes | DEV-0202 checks source pre/post hashes and companion-set stability and is not API-exposed; controlled source is expected immutable | OPEN — HIGH | Later general intake must bind immutable source storage/snapshot semantics; fail closed on any detected change |
| RSK-0006 | Cleanup can fail and leave hashed synthetic or future sensitive working material | Temporary derived data may persist outside retention policy | Context-managed cleanup, explicit audit state, surfaced cleanup failure, and DEV-0208 bounded stale-workspace recovery | OPEN — MEDIUM | Schedule recovery and add production retention, permissions, persistent audit, monitoring, and multi-process coordination |
| RSK-0007 | Approved DEV-0202 identity and corruption rules assigned different outcomes to a present but invalid SQLite `Manifest.db` | Deterministic precedence could misclassify non-Apple input as corrupt or corrupt Apple-like input as non-Apple | DEC-0008 separates independent plist identity from database validity; deterministic conflict tests cover both branches | CLOSED | Reopen if recognized identity fields change |
| RSK-0008 | DEV-0202 required contradictory encryption-indicator handling but approved only `Manifest.plist.IsEncrypted` | A fabricated secondary signal would create an unsupported forensic classification rule | DEC-0009 removes the requirement and prohibits inference from unapproved signals; DEV-0211 is deferred | CLOSED | Reopen only through an owner-approved revised compatibility profile |
| RSK-0009 | Application append-only and lock controls cannot prove storage-media immutability or legal chain-of-custody sufficiency | Users could overstate the assurance provided | ARC-002 limitations, typed services without update/delete methods, immutable records, explicit report limitations | OPEN — HIGH | Owner accepted the limitation in DEC-0014; later UI/report controls must preserve the limited terminology |
| RSK-0010 | Relational provenance can contain dangling, cyclic, or cross-tenant relationships if service boundaries are bypassed | Citations or normalized records could lose trustworthy source resolution | Tenant-scoped graph service, acyclic derivation checks, deterministic validation, policy fail-closed | OPEN — HIGH | WP-0250 conformance and later repository authorization tests |
| RSK-0011 | A parser may conform technically while its artifact behavior remains unvalidated | Interface conformance could be mistaken for forensic support | Contract status remains CANDIDATE; explicit support gate and no supported registry activation | OPEN — HIGH | Separate per-artifact validation and owner promotion |
| RSK-0012 | A committed dependency lock can age or contain a subsequently disclosed vulnerability | Reproducible installation alone does not establish dependency security | Exact reviewed pins, deterministic lock validation, isolated clean-install check, and one resolution shared by development and container builds | OPEN — MEDIUM | DEV-0106 or an approved security task must add automated vulnerability and secret scanning plus a controlled lock-refresh procedure |
| RSK-0013 | The container base image is version-tagged rather than digest-pinned | Upstream replacement of a mutable tag can prevent byte-identical container reproduction | Python patch version is fixed and Python dependencies are exactly locked | OPEN — MEDIUM | Add image-digest policy and build attestation in the approved CI/container-hardening task |
| RSK-0014 | Future code could construct registry approval metadata without binding it to an owner-controlled persistent approval record | An unapproved parser could be represented as registered if the composition boundary is bypassed | Production registry is empty; exact registry-issued authorization and output gate fail closed; no API or executor exists | OPEN — HIGH | Before any parser promotion, bind registry snapshots to tenant-scoped configuration, authorization, immutable audit, and an explicit owner approval package |
| RSK-0015 | Configuration validation cannot establish credential strength, database reachability, root permissions, or production readiness | A syntactically valid configuration may still fail operationally or use an unsafe unrecognized secret | Closed environment/driver/root policy, credential-safe diagnostics, production rejection of the documented development password | OPEN — MEDIUM | Owner accepted for WP-0100 in DEC-0019; add external secret management and deployment preflight validation in production-hardening tasks |
| RSK-0016 | Unexpected exceptions may contain evidence-derived or secret text in server logs even though API responses are safe | Sensitive content could leak through diagnostic logging | DEV-0105 uses allowlisted JSON events, credential redaction, and omits exception tracebacks/content | CLOSED | Reopen if a supported-path logger bypasses the safe formatter or logs evidence content |
| RSK-0017 | CI has not executed remotely and action references use mutable major-version tags | The local definition may behave differently in GitHub or upstream action tags could change | Workflow parses; required commands pass locally; token permission is read-only; no secrets/deploy steps | OPEN — MEDIUM | Owner accepted for WP-0100 in DEC-0019; later hardening must pin action SHAs and validate an authorized remote run |
| RSK-0018 | Foundation CI omits lint, type, vulnerability, license, secret, container, and live-database gates | Defects or dependency/security issues may pass the current regression gate | Exact dependency lock, compilation, unit/regression, architecture, and offline migration checks | OPEN — HIGH | Owner accepted for WP-0100 in DEC-0019; add approved tools in later quality/security hardening without weakening current gates |
| RSK-0019 | Deployment-selected intake ceilings may be too permissive or reject legitimate large backups | Denial-of-service exposure or avoidable intake rejection | No implicit production values; every ceiling is required and range-validated; limit denial is explicit and never a forensic compatibility classification | OPEN — HIGH | Establish environment-specific values through capacity/security validation before any production exposure |
| RSK-0020 | Coverage language or percentages could be misunderstood as completeness of all device evidence | Attorneys could overstate what a local backup or parser set establishes | Closed coverage states, explicit denominators, mandatory limitations, no device-completeness percentage | OPEN — HIGH | WP-0450 acceptance and adversarial attorney-summary tests before exposure |
| RSK-0021 | Local backup, iCloud device backup, and separately synchronized cloud services could be conflated | Collection gaps or recommendations could become misleading | FOR-009 separates source families; WP-1900 requires independent approval | OPEN — HIGH | Preserve cloud boundary in every future inventory, coverage, and recommendation rule |
| RSK-0022 | Tenant schema exists without live PostgreSQL validation or authorization repositories | Direct use could imply isolation that the deployed database and service layer have not demonstrated | Additive migration, offline PostgreSQL SQL, model constraints, no route/repository/composition | OPEN — HIGH | Validate on an approved PostgreSQL development instance and complete DEV-0310/DEV-0307 before shared workflow exposure |
